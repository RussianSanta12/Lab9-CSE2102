This Lab we did mostly in class time and for it we created a uuid checker where we pre generated a uuid and set it in place within my-server and my-calls. from there we created a new route called /uuid where the user would input their uuid and it would be checked against the preset uuid. the proper use case for uuid would be where a user puts their email in and one is generated for them along side the date and time that they first created their account. from there the next time they log in with their email it would be checked against their uuid to get them into the proper account. I then asked copilot to generate me some test cases for both my-server and my-calls
